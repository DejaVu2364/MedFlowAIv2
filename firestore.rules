rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PROTOTYPE MODE: Open rules for another 60 days
      allow read, write: if request.time < timestamp.date(2026, 3, 6);
    }
  }
}
